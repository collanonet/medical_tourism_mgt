import 'package:flutter/material.dart';

class WebAppointmentDetailScreen extends StatelessWidget {
  const WebAppointmentDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
