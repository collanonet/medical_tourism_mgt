import 'package:flutter/cupertino.dart';

class PrePatientScreen extends StatelessWidget {
  const PrePatientScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pre Patient Screen'),
    );
  }
}
