import 'package:flutter/material.dart';

class HelpAndDocScreen extends StatelessWidget {
  const HelpAndDocScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Help and Doc Screen'),);
  }
}
