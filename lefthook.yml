pre-commit:
  commands:
    pretty:
      glob: "*.dart"
      run: lefthook run fixer . && git add .


fixer:
  commands:
    dart-fix-import:
      run: fvm flutter pub run import_sorter:main
    dart-fixer:
      run: dart fix --apply